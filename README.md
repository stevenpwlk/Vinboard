# Vinboard

## Auth disabled mode

Set `AUTH_DISABLED=true` (default) to bypass authentication. The server injects a mock user and all
API routes are accessible without login. Set `AUTH_DISABLED=false` to re-enable authentication.
